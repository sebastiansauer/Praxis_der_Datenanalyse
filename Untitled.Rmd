---
output:
  pdf_document: default
  html_document: default
---


```{r echo = FALSE}
df <- read.csv("includes/Datentypen.csv")
library(xtable)
print(xtable(df, align="l|l|l|l|l|l|l"), 
      comment=F, include.rownames=F, table.placement="ht", 
      size=getOption("xtable.size", "tiny"))

```

